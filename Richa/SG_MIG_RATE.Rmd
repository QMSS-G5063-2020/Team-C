---
title: "SG_MIG_RATE"
author: "Richa Gupta"
date: "14/04/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(wbstats)
library(ggplot2)
library(dplyr)
library(scales)
library(tidyr)
setwd(getwd())

# List of iso3 country codes
need <- c("SSD", "SYR", "VEN")

# GDP Growth Rate
mig_data <- wb(country = need, indicator = "SM.POP.NETM", startdate = 2000, enddate = 2020)
```

```{r}
ggplot(mig_data, aes(x = as.factor(date), y = value, color = country)) +
  geom_line(aes(group = 1)) + geom_point() + facet_grid(rows = vars(country)) +
  labs(title = "Migration Flow",
       subtitle = "changes in migration in last 20 years", parse = TRUE) +
  xlab(NULL) + ylab(NULL)
```



## Explorations
```{r}

pov_data <- wb(country = "all", indicator = "SI.POV.NAHC", startdate = 2015, enddate = 2020)

pov_data %>%
  filter(date == 2019) %>%
  select(country, value, date) %>%
  arrange(value)

```